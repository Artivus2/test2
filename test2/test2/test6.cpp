
#include <iostream>
#include <windows.h> 
#include <string>
#include <stdlib.h>
using namespace std;



int main_6() {

	system("chcp 1251");
	
	

	/*string number;
	string res1, res2, temp;
	cout << "Введите 4-е число: ";
	cin >> number;
	temp = number[1] + number[0];
	cout << "Итоговое число: " << temp;*/

	//cout << number.length();
	/*if (number.length() != 6) {
		cout << "Ввели не вверное количество знаков!";
		exit;
	}
	else {
		res1 = (int)(number[0] - '0' + number[1] - '0' + number[2] - '0');
		res2 = (int)(number[3] - '0' + number[4] - '0' + number[5] - '0');
		if (res1 == res2) {
			cout << "Число счастливое";
		}
		else
		{
			cout << "Число не счастливое";
		}
		cout << res1 << " : " << res2;
	}*/

	/*string number;

	cin >> number;

	if (number.length() != 4) {
		cout << "Ввели не вверное количество знаков!";
		exit;
	} else {
		string rez { number[1], number[0], number[3], number[2] };

		cout << "Итоговое число: " << rez;*/

	/*int a, maxnum;
	cin >> a;
	maxnum = a;
	for (int i = 0; i < 7; i++) {
		cin >> a;
		(maxnum >= a) ? maxnum = maxnum : maxnum = a;
	}*/
	/*cin >> a;
	(maxnum >= a) ? true : maxnum = a;
	cin >> a;
	(maxnum >= a) ? true : maxnum = a;
	cin >> a;
	(maxnum >= a) ? true : maxnum = a;
	cin >> a;
	(maxnum >= a) ? true : maxnum = a;
	cin >> a;
	(maxnum >= a) ? true : maxnum = a;
	cin >> a;
	(maxnum >= a) ? true : maxnum = a;
	cin >> a;
	(maxnum >= a) ? true : maxnum = a;*/
	
	//cout << "\n" << maxnum;
	

	//int p1(1), p2(2), p3(3), p4(4), j1(1), j2(2), j3(3);
	//float pp1(100.5), pp2(70.5), pp3(110.8), pp4(60), pj1(30), pj2(35), pj(23);
	//int countpz, countj, nump, numj;
	//float itogp = 0, itogj = 0, itog = 0;
	//string namep, namej;
	//float cashbackj = 15;
	//float cashback = 20;
	//cout << "Выберите пиццу: ";
	//cin >> nump;
	//cout << "Выберите количество: ";
	//cin >> countpz;
	//if (nump == p1) {
	//	itogp = pp1 * countpz;
	//	namep = "Пепперони";

	//}
	////для также
	//cout << "Выберите напиток: ";
	//cin >> numj;
	//cout << "Выберите колисество: ";
	//cin >> countj;
	//if (numj == j1) {
	//	itogj = pj1 * countj;
	//	namej = "Яблочный сок";
	//}

	////countpz = (int)countpz / 5;

	//itogp -= pp1 * ((int)countpz / 5);



	//if (pj1 > 27 && countj > 3) {
	//	itogj *= (1 - cashbackj / 100);
	//	cout << "скидка по соку прошла";
	//
	//};

	//itog = itogp + itogj;

	//if (itog > 500) {
	//	itog *= (1 - cashback / 100);
	//	cout << "общая скидка прошла";
	//}

	//cout << namep << " : " << countpz << " : " << itogp << "\n";
	//cout << namej << " : " << countj << " : " << itogj << "\n";
	//cout << "\t\t\t" << itog << "\n";

return 0;



}